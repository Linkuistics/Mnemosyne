### Session 17 (2026-04-16T00:01:30Z) — Sub-E amendment: Stage 5 dispatch-to-experts (spec rewrite)

- **What was attempted:** Inline-rewrite of `docs/superpowers/specs/2026-04-12-sub-E-ingestion-design.md` to absorb the BEAM/actor pivot (orchestrator Session 9) and Stage 5 expert-dispatch contracts (sub-N Session 16). Per the "amendments rewrite inline, not as supersede layers" discipline — no supersede block appended, §-numbering preserved across §1–§5 + Appendix A for downstream cross-reference stability.
- **What worked:**
  - All §1–§5 sections re-cast to BEAM/Elixir without breaking section numbering. Sub-M's §9 citation of sub-E events stays valid.
  - Decision Trail gained Q15 (BEAM pivot) and Q16 (Stage 5 expert dispatch) as fresh entries; correction notes folded back into Q1/Q2/Q5/Q9/Q10/Q11/Q13 at their original question entries (per the "history at the original question entry" half of the discipline).
  - Concrete contracts wired: `Mnemosyne.ReflectExitHook` callback shape from sub-B §4.2 (with `set_fired` closure flow); `GenStage` pipeline rooted at `Mnemosyne.Ingestion.Pipeline`; struct types with `@enforce_keys`; `Mnemosyne.Expert.ScopeMatcher.match_candidate/2` call site (sub-N §6.1); `%Mnemosyne.Message.ExpertAbsorbCandidate{}` dispatch shape; the three verdict variants (`:absorb`/`:reject`/`:cross_link_suggested`); non-recursive max-depth-2 cross-link follow-up with `%Expert.CrossLinkDeadEnd{}`; `%Expert.Conflict{}` for contentful disagreement (≥10 char reject reason heuristic from sub-N §6.3); orphan path → `<vault>/knowledge/uncategorized/` via `SafeFileWriter` emitting sub-E-owned `%Mnemosyne.Event.Ingestion.OrphanCandidate{}`.
  - **Sub-M parallel-emit window collapses to zero.** Sub-E no longer has a standalone `IngestionEvent` channel to wrap — it emits directly to sub-M's bus from Stage 5. Sub-M's adoption matrix entry for sub-E becomes "verify schema" rather than "wrap and migrate." This is a clean simplification that wasn't obvious until the rewrite was in flight.
  - Prompt resolution recast as **request-response over sub-M's bus + sub-K NDJSON command** rather than an embedded `oneshot` channel — sub-E's pipeline GenServer holds an outstanding-prompts map keyed by `candidate_id`. This dropped a Rust idiom that wasn't transplantable cleanly.
  - Cross-sub-project requirements section restructured per-sibling (B/C/A/D/F/N/M/H) instead of the original generic bullet list — much easier for siblings to absorb during their own brainstorms/amendments.
  - Risk list grew from 4 to 6: Risks 4 (OTP mailbox backpressure under burst dispatch) and 5 (`:telemetry` handler crash blinding the bus, per sub-M §5.2) added; Risk 1 (LLM latency) updated to note that Stage 5 expert curation parallelizes naturally across `ExpertActor` processes so wall-time scales as `max(K × per-curation latency)`, not `K × latency`.
  - Verification clean — only legitimate "Rust" references survive (the Rust TUI as v1 client, the Rust-CLI historical mention in §1, the Q15 BEAM-pivot entry). No residual `Vec<>`, `tokio`, `mpsc`, `trait`, `PathBuf`, `enum`-as-rust-keyword, etc.
- **What didn't work:**
  - Nothing failed, but the spec rewrite revealed that **sub-E's sibling-plan implementation task list still carries pre-pivot Rust framing** (`Vec<Section>`, `tokio mpsc`, trait objects, `IngestionEvent` channel, `SafeFileWriter` over both tiers). That backlog needs its own task-list rewrite mirroring the sub-B/sub-C/sub-M gate-task pattern before any implementation work can be picked up.
- **What this suggests trying next:**
  - **Highest-leverage next move:** create a sub-E-internal task-list rewrite gate task (analog of sub-B Task 0, sub-C Task 0, sub-M Task 0). The orchestrator backlog already notes this in the Sub-E amendment Results; triage should surface it as a new sub-E backlog item.
  - The remaining Priority-1 amendments (D, G, H, I) are all small spec edits and can run in any order — none of them blocks v1 critical path now that the F+N integration shape is locked.
  - Sub-N implementation can proceed against the rewritten contract immediately; sub-N's own backlog Tasks 16+ are no longer waiting on this amendment.
- **Key learnings or discoveries:**
  - **The "rewrite inline" discipline composes with "concrete-contracts-from-upstream" cleanly.** Having sub-N §6 + §9.4 spelled out in code-shaped form (struct names, function specs, verdict atoms) made the rewrite mechanical — there was almost no judgment work involved at the contract layer. Brainstorm exit criteria that produce concrete code-shaped contracts are dramatically more useful for downstream amendments than prose-shaped commitments.
  - **The parallel-emit transition pattern (sub-M's safety net) collapsing to zero is a strong signal that sub-E was rewritten cleanly in-place.** The pattern exists for cases where the migration is risky enough to warrant dual-emission for verification; when the rewrite is fully contract-driven and the new emission path is statically equivalent to the old, the safety net isn't needed. This is a design-time signal worth watching for in the remaining amendments.
  - **Sub-M's sealed event set was the single largest lever in the rewrite.** Without `%Mnemosyne.Event.Ingestion.*` already declared in sub-M §4.1 (with field-level definitions from the Session 15 inline rewrite), sub-E's amendment would have had to invent event shapes that sub-M would then need to absorb in its own amendment cycle. The sealed-set discipline is paying compound interest across amendments.
  - **Cross-sub-project requirements restructured per-sibling** is a clearer pattern than the original "flat bullet list" form. It lets each sibling read sub-E's spec and find their own commitments in one place. Worth replicating in future amendments and brainstorms.
